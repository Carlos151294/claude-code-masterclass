{
  "permissions": {
    "allow": [
      "WebSearch",
      "Bash(git init *)",
      "Bash(git switch *)",
      "Bash(git commit *)",
      "Bash(git checkout *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read fp; [[ \"$fp\" =~ \\.tsx?$ ]] && npx prettier --write \"$fp\"; }"
          }
        ]
      }
    ]
  }
}
